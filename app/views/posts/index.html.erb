<h1>投稿一覧表示</h1>
<table>
    <tr>
        <th>ID</th>
        <th>カテゴリー</th>
        <th>投稿者名</th>
        <th>内容</th>
    </tr>
    <% @posts.each do |post| %>
      <tr>
          <td><%= post.id %></td>
          <td><%= post.category %></td>
          <td><%= post.user.name %></td>
          <td><%= post.body %></td>
         
      </tr>
    <% end %>
</table>