<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <P><%= @user.name %></P>
      <%= form_with model: @block, url: blocks_path(@user), method: :post, class: "form" do |f| %>
        <div class="form-check mb-3">
        </div>
        <%= f.submit "ブロック", class: "btn btn-danger" %>
        
    <% end %>
    </div>
  </div>
</div>